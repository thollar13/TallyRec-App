<%= form_for(@game_event) do |f| %>
  <% if @game_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_event.errors.count, "error") %> prohibited this game_event from being saved:</h2>

      <ul>
      <% @game_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<table>
  <tr>
    <td colspan="6">
      <%= f.label :title %> <%= f.text_field :title %>
    </td>
  </tr>
  <tr>
    <td> Home: <br><%= f.collection_select :home_team_id, Team.all, :id, :name %></td>
    <td>
      <%= f.label :home_score %><br>
      <%= f.text_field :home_score %>
    </td>
  </tr>
  <tr>
    <td>
     Away: <br><%= f.collection_select :away_team_id, Team.all, :id, :name %>
    </td>
    <td>
      <%= f.label :away_score %><br>
      <%= f.text_field :away_score %>
    </td>
  </tr>
  <tr>
    <td colspan="3">
      <%= f.label :start_date %><br>
      <%= f.datetime_select :start_date %></td>
    <td colspan="3">
      <%= f.label :location %><br>
      <%= f.text_area :location %>
    </td>
  </tr>
</table>
   <%= f.collection_select :league_id, @leagues, :id, :name %>
 <!--  <div class="field">
   
  </div>
  <div class="field">
    <%= f.label :end_date %><br>
    <%= f.datetime_select :end_date %>
  </div> -->
 
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
